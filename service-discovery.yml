server:
  port: 8083
spring:
  application:
    name: service-discovery
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url.defaultZone: http://localhost:8083/eureka
